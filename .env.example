# Example client-side environment variables for Vite + Supabase
# Copy this file to `.env` and replace the values with your project's keys.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-api-key

# Optional: redirect URL used by Supabase Auth (set in Supabase dashboard)
# VITE_APP_REDIRECT_URL=http://localhost:5173

# IMPORTANT: Do NOT commit your service role key. If you need server-side
# operations that require elevated privileges, store SUPABASE_SERVICE_ROLE_KEY
# in a server-only environment (not in Vite / client .env files).
